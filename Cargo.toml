[workspace]
members = [
    "quil-rs",
    "quil-py"
]

[profile.release]
lto = true
codegen-units = 1

[workspace.dependencies]
serde = { version = "1.0.125", features = ["derive"] }
num-complex = { version = "0.4.0", features = ["serde"] }
